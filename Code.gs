/**
 * Скрипт для суммирования финансовых транзакций по категориям
 * Считывает диапазон дат с листа "AI" и суммирует данные с листа "Все"
 */

// ============================================================================
// КОНФИГУРАЦИЯ (Улучшение #3: Настройка колонок через конфигурацию)
// ============================================================================
var КОНФИГ = {
  // Имена листов
  ЛИСТ_РЕЗУЛЬТАТОВ: 'AI',
  ЛИСТ_ДАННЫХ: 'Все',
  
  // Колонки на листе "Все"
  КОЛОНКИ: {
    ДАТА_ПЛАТЕЖА: 2,    // B
    НОМЕР_КАРТЫ: 3,     // C
    СУММА_ПЛАТЕЖА: 7,   // G
    КАТЕГОРИЯ: 10       // J
  },
  
  // Первая строка данных (1 = заголовки, 2 = данные)
  ПЕРВАЯ_СТРОКА_ДАННЫХ: 2,
  
  // Настройки вывода результатов
  РЕЗУЛЬТАТ_СТАРТ_СТРОКА: 2,
  РЕЗУЛЬТАТ_СТАРТ_КОЛОНКА: 1,
  
  // Год для обработки (null = определяется автоматически из данных)
  ГОД: null,
  
  // Разрешенные номера карт
  РАЗРЕШЕННЫЕ_КАРТЫ: ['*4963', '*5436', '*6058'],
  
  // Названия карт
  НАЗВАНИЯ_КАРТ: {
    '*4963': '',
    '*5436': 'Красный кошелек',
    '*6058': 'Зеленый кошелек'
  }
};

// ============================================================================
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ============================================================================

/**
 * Преобразует значение ячейки в дату
 */
function получитьДату(значение) {
  if (!значение) return null;
  
  // Если это уже объект Date
  if (значение instanceof Date) {
    return значение;
  }
  
  // Если это строка, пытаемся преобразовать
  if (typeof значение === 'string') {
    // Формат DD.MM.YYYY
    var части = значение.split('.');
    if (части.length === 3) {
      return new Date(части[2], части[1] - 1, части[0]);
    }
    // Пробуем стандартное преобразование
    var дата = new Date(значение);
    if (!isNaN(дата.getTime())) {
      return дата;
    }
  }
  
  return null;
}

/**
 * Преобразует значение суммы в число
 * Убирает текст "RUB", запятые заменяет на точки
 */
function получитьСумму(значение) {
  if (значение === null || значение === undefined || значение === '') {
    return 0;
  }
  
  // Если это уже число
  if (typeof значение === 'number') {
    return значение;
  }
  
  // Если это строка, извлекаем число
  var текст = значение.toString();
  // Убираем все кроме цифр, минуса и запятой/точки
  текст = текст.replace(/[^\d.,-]/g, '');
  // Заменяем запятую на точку
  текст = текст.replace(',', '.');
  
  var сумма = parseFloat(текст);
  return isNaN(сумма) ? 0 : сумма;
}

/**
 * Получает название карты для отображения (Улучшение #3)
 */
function получитьНазваниеКарты(номерКарты) {
  var название = КОНФИГ.НАЗВАНИЯ_КАРТ[номерКарты] || '';
  if (название) {
    return номерКарты + ' - ' + название;
  }
  return номерКарты;
}

/**
 * Генерирует массив месяцев для указанного года (Улучшение #2: Динамическая генерация месяцев)
 */
function сгенерироватьМесяцы(год) {
  var месяцы = [];
  var названияМесяцев = [
    'Январь', 'Февраль', 'Март', 'Апрель', 'Май', 'Июнь',
    'Июль', 'Август', 'Сентябрь', 'Октябрь', 'Ноябрь', 'Декабрь'
  ];
  
  for (var месяц = 0; месяц < 12; месяц++) {
    var первыйДень = new Date(год, месяц, 1);
    var последнийДень = new Date(год, месяц + 1, 0); // 0 день следующего месяца = последний день текущего
    
    var деньНачала = ('0' + первыйДень.getDate()).slice(-2);
    var деньКонца = ('0' + последнийДень.getDate()).slice(-2);
    var месяцСтрока = ('0' + (месяц + 1)).slice(-2);
    
    месяцы.push({
      название: названияМесяцев[месяц] + ' ' + год,
      начало: деньНачала + '.' + месяцСтрока + '.' + год,
      конец: деньКонца + '.' + месяцСтрока + '.' + год,
      начальнаяДата: первыйДень,
      конечнаяДата: последнийДень
    });
  }
  
  return месяцы;
}

/**
 * Определяет год из данных или использует текущий (Улучшение #2)
 */
function определитьГод(данные) {
  if (КОНФИГ.ГОД) {
    return КОНФИГ.ГОД;
  }
  
  var текущийГод = new Date().getFullYear();
  var найденныйГод = null;
  
  // Ищем первый год в данных
  for (var i = 0; i < данные.length; i++) {
    if (данные[i].дата) {
      var год = данные[i].дата.getFullYear();
      if (!найденныйГод || год < найденныйГод) {
        найденныйГод = год;
      }
    }
  }
  
  return найденныйГод || текущийГод;
}

/**
 * Читает все данные с листа один раз (Улучшение #1: Оптимизация производительности)
 */
function прочитатьВсеДанные(лист, config) {
  try {
    var всеLastRow = лист.getLastRow();
    if (всеLastRow < config.ПЕРВАЯ_СТРОКА_ДАННЫХ) {
      Logger.log('Нет данных для чтения');
      return [];
    }
    
    var данные = [];
    var колонки = config.КОЛОНКИ;
    
    // Читаем все данные одним батчем для оптимизации
    var диапазон = лист.getRange(config.ПЕРВАЯ_СТРОКА_ДАННЫХ, 1, всеLastRow - config.ПЕРВАЯ_СТРОКА_ДАННЫХ + 1, 10);
    var значения = диапазон.getValues();
    
    for (var i = 0; i < значения.length; i++) {
      var строка = значения[i];
      var датаПлатежа = строка[колонки.ДАТА_ПЛАТЕЖА - 1];
      var номерКарты = строка[колонки.НОМЕР_КАРТЫ - 1];
      var суммаПлатежа = строка[колонки.СУММА_ПЛАТЕЖА - 1];
      var категория = строка[колонки.КАТЕГОРИЯ - 1];
      
      // Пропускаем пустые строки
      if (!датаПлатежа || !номерКарты || номерКарты === '' || !категория || категория === '') {
        continue;
      }
      
      // Определяем карту
      var найденнаяКарта = null;
      for (var j = 0; j < config.РАЗРЕШЕННЫЕ_КАРТЫ.length; j++) {
        if (String(номерКарты).endsWith(config.РАЗРЕШЕННЫЕ_КАРТЫ[j])) {
          найденнаяКарта = config.РАЗРЕШЕННЫЕ_КАРТЫ[j];
          break;
        }
      }
      
      if (!найденнаяКарта) {
        continue;
      }
      
      // Преобразуем дату
      var дата = получитьДату(датаПлатежа);
      if (!дата) {
        Logger.log('Не удалось преобразовать дату в строке ' + (config.ПЕРВАЯ_СТРОКА_ДАННЫХ + i) + ': ' + датаПлатежа);
        continue;
      }
      
      // Получаем сумму
      var сумма = получитьСумму(суммаПлатежа);
      if (сумма === 0) {
        continue;
      }
      
      данные.push({
        дата: дата,
        карта: найденнаяКарта,
        категория: String(категория).trim(),
        сумма: сумма,
        номерСтроки: config.ПЕРВАЯ_СТРОКА_ДАННЫХ + i
      });
    }
    
    Logger.log('Прочитано ' + данные.length + ' транзакций');
    return данные;
    
  } catch (error) {
    Logger.log('Ошибка при чтении данных: ' + error.toString());
    throw error;
  }
}

/**
 * Группирует данные по месяцам и картам (Улучшение #1)
 */
function группироватьДанныеПоМесяцам(данные, месяцы, разрешенныеКарты) {
  var результат = {};
  
  // Инициализируем структуру для каждого месяца
  for (var i = 0; i < месяцы.length; i++) {
    var месяц = месяцы[i];
    var ключМесяца = месяц.название;
    
    результат[ключМесяца] = {};
    for (var j = 0; j < разрешенныеКарты.length; j++) {
      var карта = разрешенныеКарты[j];
      результат[ключМесяца][карта] = {
        общаяСумма: 0,
        категории: {}
      };
    }
  }
  
  // Группируем данные
  for (var i = 0; i < данные.length; i++) {
    var транзакция = данные[i];
    var дата = транзакция.дата;
    
    // Находим месяц
    for (var j = 0; j < месяцы.length; j++) {
      var месяц = месяцы[j];
      var начальнаяДата = new Date(месяц.начальнаяДата);
      начальнаяДата.setHours(0, 0, 0, 0);
      var конечнаяДата = new Date(месяц.конечнаяДата);
      конечнаяДата.setHours(23, 59, 59, 999);
      
      if (дата.getTime() >= начальнаяДата.getTime() && дата.getTime() <= конечнаяДата.getTime()) {
        var ключМесяца = месяц.название;
        var карта = транзакция.карта;
        var категория = транзакция.категория;
        var сумма = транзакция.сумма;
        
        if (!результат[ключМесяца][карта].категории[категория]) {
          результат[ключМесяца][карта].категории[категория] = 0;
        }
        
        результат[ключМесяца][карта].общаяСумма += сумма;
        результат[ключМесяца][карта].категории[категория] += сумма;
        break;
      }
    }
  }
  
  return результат;
}

/**
 * Применяет форматирование к диапазону ячеек
 */
function применитьФорматирование(range, стиль) {
  if (стиль.fontWeight) range.setFontWeight(стиль.fontWeight);
  if (стиль.fontSize) range.setFontSize(стиль.fontSize);
  if (стиль.fontFamily) range.setFontFamily(стиль.fontFamily);
  if (стиль.fontColor) range.setFontColor(стиль.fontColor);
  if (стиль.backgroundColor) range.setBackground(стиль.backgroundColor);
  if (стиль.horizontalAlignment) range.setHorizontalAlignment(стиль.horizontalAlignment);
  if (стиль.verticalAlignment) range.setVerticalAlignment(стиль.verticalAlignment);
  if (стиль.border !== undefined && стиль.border) {
    range.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
  }
}

/**
 * Основная функция для суммирования по категориям (переработанная с улучшениями)
 * Улучшения: #1 Оптимизация, #2 Динамический год, #3 Конфигурация, #4 Батчинг, #5 Обработка ошибок
 */
function Test() {
  try {
    Logger.log('=== Начало выполнения скрипта ===');
    
    var spreadsheet = SpreadsheetApp.getActiveSpreadsheet();
    
    // Получаем листы (Улучшение #5: Обработка ошибок)
    var aiSheet = spreadsheet.getSheetByName(КОНФИГ.ЛИСТ_РЕЗУЛЬТАТОВ);
    if (!aiSheet) {
      var ошибка = 'Лист "' + КОНФИГ.ЛИСТ_РЕЗУЛЬТАТОВ + '" не найден!';
      Logger.log('ОШИБКА: ' + ошибка);
      SpreadsheetApp.getUi().alert(ошибка);
      return;
    }
    
    var всеSheet = spreadsheet.getSheetByName(КОНФИГ.ЛИСТ_ДАННЫХ);
    if (!всеSheet) {
      var ошибка = 'Лист "' + КОНФИГ.ЛИСТ_ДАННЫХ + '" не найден!';
      Logger.log('ОШИБКА: ' + ошибка);
      SpreadsheetApp.getUi().alert(ошибка);
      return;
    }
    
    // Очищаем весь лист "AI" (включая форматирование)
    aiSheet.clear();
    Logger.log('Лист результатов очищен');
    
    // Улучшение #1: Читаем все данные один раз
    Logger.log('Чтение данных с листа "' + КОНФИГ.ЛИСТ_ДАННЫХ + '"...');
    var всеДанные = прочитатьВсеДанные(всеSheet, КОНФИГ);
    
    if (всеДанные.length === 0) {
      Logger.log('Нет данных для обработки');
      SpreadsheetApp.getUi().alert('На листе "' + КОНФИГ.ЛИСТ_ДАННЫХ + '" нет данных для обработки');
      return;
    }
    
    // Улучшение #2: Определяем год и генерируем месяцы динамически
    var год = определитьГод(всеДанные);
    Logger.log('Определен год: ' + год);
    var месяцы = сгенерироватьМесяцы(год);
    Logger.log('Сгенерировано ' + месяцы.length + ' месяцев');
    
    // Улучшение #1: Группируем данные по месяцам один раз
    Logger.log('Группировка данных по месяцам...');
    var данныеПоМесяцам = группироватьДанныеПоМесяцам(всеДанные, месяцы, КОНФИГ.РАЗРЕШЕННЫЕ_КАРТЫ);
    
    // Настройки вывода
    var результатStartRow = КОНФИГ.РЕЗУЛЬТАТ_СТАРТ_СТРОКА;
    var результатStartCol = КОНФИГ.РЕЗУЛЬТАТ_СТАРТ_КОЛОНКА;
    var текущаяСтрока = результатStartRow;
  
    
    // Обрабатываем каждый месяц
    for (var месяцIdx = 0; месяцIdx < месяцы.length; месяцIdx++) {
      var месяц = месяцы[месяцIdx];
      var данныеПоКартам = данныеПоМесяцам[месяц.название];
      
      if (!данныеПоКартам) {
        continue;
      }
      
      // Проверяем, есть ли данные для этого месяца
      var естьДанные = false;
      for (var карта in данныеПоКартам) {
        if (данныеПоКартам[карта].общаяСумма !== 0) {
          естьДанные = true;
          break;
        }
      }
      
      // Пропускаем месяц, если нет данных
      if (!естьДанные) {
        continue;
      }
      
      Logger.log('Обработка месяца: ' + месяц.название);
      
      // Добавляем заголовок месяца только если есть данные
      if (текущаяСтрока > результатStartRow) {
        текущаяСтрока += 2; // Пустые строки между месяцами
      }
      
      var заголовокМесяцаRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 3);
      заголовокМесяцаRange.merge();
      var заголовокЯчейка = aiSheet.getRange(текущаяСтрока, результатStartCol);
      заголовокЯчейка.setValue('📅 ' + месяц.название);
      заголовокЯчейка.setFontWeight('bold');
      заголовокЯчейка.setFontSize(16);
      заголовокЯчейка.setFontFamily('Arial');
      заголовокЯчейка.setHorizontalAlignment('center');
      заголовокЯчейка.setBackground('#D9E1F2');
      заголовокМесяцаRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
      текущаяСтрока++;
      текущаяСтрока++;
      
      // Улучшение #4: Батчинг - собираем данные для таблицы общих итогов
      var общаяСуммаВсехТрат = 0;
      var строкиДанных = [];
      for (var i = 0; i < КОНФИГ.РАЗРЕШЕННЫЕ_КАРТЫ.length; i++) {
        var карта = КОНФИГ.РАЗРЕШЕННЫЕ_КАРТЫ[i];
        var данные = данныеПоКартам[карта];
        var суммаКарты = Math.abs(данные.общаяСумма);
        общаяСуммаВсехТрат += суммаКарты;
        var процент = 0; // Вычислим после
        строкиДанных.push([получитьНазваниеКарты(карта), суммаКарты, процент]);
      }
      
      // Вычисляем проценты
      for (var i = 0; i < строкиДанных.length; i++) {
        if (общаяСуммаВсехТрат > 0) {
          строкиДанных[i][2] = (строкиДанных[i][1] / общаяСуммаВсехТрат * 100);
        }
      }
      
      // Улучшение #4: Записываем заголовки батчем
      var заголовкиRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 3);
      заголовкиRange.setValues([['Номер карты', 'Общая сумма', 'Процент']]);
      применитьФорматирование(заголовкиRange, {
        fontWeight: 'bold',
        fontSize: 12,
        fontFamily: 'Arial',
        fontColor: '#FFFFFF',
        backgroundColor: '#4472C4',
        horizontalAlignment: 'center',
        verticalAlignment: 'middle',
        border: true
      });
      текущаяСтрока++;
      
      // Улучшение #4: Записываем данные батчем
      if (строкиДанных.length > 0) {
        var данныеRange = aiSheet.getRange(текущаяСтрока, результатStartCol, строкиДанных.length, 3);
        данныеRange.setValues(строкиДанных);
        
        // Форматирование для каждой строки
        for (var i = 0; i < строкиДанных.length; i++) {
          var строкаRange = aiSheet.getRange(текущаяСтрока + i, результатStartCol, 1, 3);
          aiSheet.getRange(текущаяСтрока + i, результатStartCol + 1).setNumberFormat('#,##0.00" RUB"');
          aiSheet.getRange(текущаяСтрока + i, результатStartCol + 2).setNumberFormat('0.00"%"');
          применитьФорматирование(строкаRange, {
            fontFamily: 'Arial',
            fontSize: 11,
            backgroundColor: (i % 2 === 0) ? '#D9E1F2' : '#FFFFFF',
            border: true
          });
        }
        
        текущаяСтрока += строкиДанных.length;
      }
  
      // Пустая строка после таблицы общих итогов
      текущаяСтрока++;
      текущаяСтрока++;
      
      // 2. Выводим категории для каждой карты
      for (var i = 0; i < КОНФИГ.РАЗРЕШЕННЫЕ_КАРТЫ.length; i++) {
        var карта = КОНФИГ.РАЗРЕШЕННЫЕ_КАРТЫ[i];
        var данные = данныеПоКартам[карта];
        
        // Получаем категории и сортируем по сумме (по убыванию)
        var категорииКарты = Object.keys(данные.категории);
        if (категорииКарты.length > 0) {
          категорииКарты.sort(function(a, b) {
            return данные.категории[b] - данные.категории[a];
          });
          
          // Заголовок таблицы для карты
          var заголовокКартыRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 2);
          заголовокКартыRange.merge();
          aiSheet.getRange(текущаяСтрока, результатStartCol).setValue('💳 Карта: ' + получитьНазваниеКарты(карта));
          применитьФорматирование(aiSheet.getRange(текущаяСтрока, результатStartCol), {
            fontWeight: 'bold',
            fontSize: 13,
            fontFamily: 'Arial',
            horizontalAlignment: 'center',
            backgroundColor: '#F2F2F2',
            border: true
          });
          текущаяСтрока++;
          
          // Улучшение #4: Заголовки категорий батчем
          var заголовкиКатегорийRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 2);
          заголовкиКатегорийRange.setValues([['Категория', 'Сумма']]);
          применитьФорматирование(заголовкиКатегорийRange, {
            fontWeight: 'bold',
            fontSize: 11,
            fontFamily: 'Arial',
            fontColor: '#000000',
            backgroundColor: '#FFC000',
            horizontalAlignment: 'center',
            verticalAlignment: 'middle',
            border: true
          });
          текущаяСтрока++;
          
          // Улучшение #4: Собираем данные категорий для батча
          var данныеКатегорий = [];
          for (var j = 0; j < категорииКарты.length; j++) {
            var категория = категорииКарты[j];
            var сумма = Math.abs(данные.категории[категория]);
            данныеКатегорий.push([категория, сумма]);
          }
          
          // Записываем категории батчем
          if (данныеКатегорий.length > 0) {
            var категорииRange = aiSheet.getRange(текущаяСтрока, результатStartCol, данныеКатегорий.length, 2);
            категорииRange.setValues(данныеКатегорий);
            
            // Форматирование для каждой строки категорий
            for (var j = 0; j < данныеКатегорий.length; j++) {
              var строкаКатегорииRange = aiSheet.getRange(текущаяСтрока + j, результатStartCol, 1, 2);
              aiSheet.getRange(текущаяСтрока + j, результатStartCol + 1).setNumberFormat('#,##0.00" RUB"');
              применитьФорматирование(строкаКатегорииRange, {
                fontFamily: 'Arial',
                fontSize: 10,
                backgroundColor: (j % 2 === 0) ? '#FFF2CC' : '#FFE699',
                border: true
              });
            }
            
            текущаяСтрока += данныеКатегорий.length;
          }
          
          // Пустая строка после таблицы карты
          текущаяСтрока++;
        }
      }
    }
    
    // Автоподбор ширины колонок (3 колонки для таблицы итогов)
    aiSheet.autoResizeColumns(результатStartCol, 3);
    
    Logger.log('=== Выполнение скрипта успешно завершено ===');
    SpreadsheetApp.getUi().alert('Суммирование завершено для всех месяцев!');
    
  } catch (error) {
    // Улучшение #5: Обработка ошибок
    var сообщение = 'Произошла ошибка при выполнении скрипта: ' + error.toString();
    Logger.log('КРИТИЧЕСКАЯ ОШИБКА: ' + сообщение);
    Logger.log('Стек вызовов: ' + error.stack);
    SpreadsheetApp.getUi().alert(сообщение);
    throw error;
  }
}

/**
 * Функция для создания меню в Google Sheets
 */
function onOpen() {
  var ui = SpreadsheetApp.getUi();
  ui.createMenu('📊 Финансы')
    .addItem('Суммировать по категориям', 'Test')
    .addToUi();
  }
