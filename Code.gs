/**
 * Скрипт для суммирования финансовых транзакций по категориям
 * Считывает диапазон дат с листа "AI" и суммирует данные с листа "Все"
 */

/**
 * Преобразует значение ячейки в дату
 */
function получитьДату(значение) {
  if (!значение) return null;
  
  // Если это уже объект Date
  if (значение instanceof Date) {
    return значение;
  }
  
  // Если это строка, пытаемся преобразовать
  if (typeof значение === 'string') {
    // Формат DD.MM.YYYY
    var части = значение.split('.');
    if (части.length === 3) {
      return new Date(части[2], части[1] - 1, части[0]);
    }
    // Пробуем стандартное преобразование
    var дата = new Date(значение);
    if (!isNaN(дата.getTime())) {
      return дата;
    }
  }
  
  return null;
}

/**
 * Преобразует значение суммы в число
 * Убирает текст "RUB", запятые заменяет на точки
 */
function получитьСумму(значение) {
  if (значение === null || значение === undefined || значение === '') {
    return 0;
  }
  
  // Если это уже число
  if (typeof значение === 'number') {
    return значение;
  }
  
  // Если это строка, извлекаем число
  var текст = значение.toString();
  // Убираем все кроме цифр, минуса и запятой/точки
  текст = текст.replace(/[^\d.,-]/g, '');
  // Заменяем запятую на точку
  текст = текст.replace(',', '.');
  
  var сумма = parseFloat(текст);
  return isNaN(сумма) ? 0 : сумма;
}

/**
 * Основная функция для суммирования по категориям для всех 12 месяцев 2025 года
 */
function Test() {
  var spreadsheet = SpreadsheetApp.getActiveSpreadsheet();
  
  // Получаем лист "AI"
  var aiSheet = spreadsheet.getSheetByName('AI');
  if (!aiSheet) {
    SpreadsheetApp.getUi().alert('Лист "AI" не найден!');
    return;
  }
  
  // Получаем лист "Все"
  var всеSheet = spreadsheet.getSheetByName('Все');
  if (!всеSheet) {
    SpreadsheetApp.getUi().alert('Лист "Все" не найден!');
    return;
  }
  
  // Очищаем весь лист "AI" (включая форматирование)
  aiSheet.clear();
  
  // Определяем, куда выводить результаты
  var результатStartRow = 2; // Начинаем со второй строки
  var результатStartCol = 1; // Колонка A
  
  var текущаяСтрока = результатStartRow;
  
  // Массив месяцев 2025 года (название, начальная дата, конечная дата)
  var месяцы = [
    { название: 'Январь 2025', начало: '01.01.2025', конец: '31.01.2025' },
    { название: 'Февраль 2025', начало: '01.02.2025', конец: '28.02.2025' },
    { название: 'Март 2025', начало: '01.03.2025', конец: '31.03.2025' },
    { название: 'Апрель 2025', начало: '01.04.2025', конец: '30.04.2025' },
    { название: 'Май 2025', начало: '01.05.2025', конец: '31.05.2025' },
    { название: 'Июнь 2025', начало: '01.06.2025', конец: '30.06.2025' },
    { название: 'Июль 2025', начало: '01.07.2025', конец: '31.07.2025' },
    { название: 'Август 2025', начало: '01.08.2025', конец: '31.08.2025' },
    { название: 'Сентябрь 2025', начало: '01.09.2025', конец: '30.09.2025' },
    { название: 'Октябрь 2025', начало: '01.10.2025', конец: '31.10.2025' },
    { название: 'Ноябрь 2025', начало: '01.11.2025', конец: '30.11.2025' },
    { название: 'Декабрь 2025', начало: '01.12.2025', конец: '31.12.2025' }
  ];
  
  // Обрабатываем каждый месяц
  for (var месяцIdx = 0; месяцIdx < месяцы.length; месяцIdx++) {
    var месяц = месяцы[месяцIdx];
    var начальнаяДата = получитьДату(месяц.начало);
    var конечнаяДата = получитьДату(месяц.конец);
    
    if (!начальнаяДата || !конечнаяДата) {
      continue;
    }
    
    // Устанавливаем время для корректного сравнения (начало и конец дня)
    начальнаяДата.setHours(0, 0, 0, 0);
    конечнаяДата.setHours(23, 59, 59, 999);
    
    // Добавляем заголовок месяца
    if (месяцIdx > 0) {
      текущаяСтрока += 2; // Пустые строки между месяцами
    }
    
    var заголовокМесяцаRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 3);
    заголовокМесяцаRange.merge();
    aiSheet.getRange(текущаяСтрока, результатStartCol).setValue('📅 ' + месяц.название);
    aiSheet.getRange(текущаяСтрока, результатStartCol).setFontWeight('bold');
    aiSheet.getRange(текущаяСтрока, результатStartCol).setFontSize(16);
    aiSheet.getRange(текущаяСтрока, результатStartCol).setFontFamily('Arial');
    aiSheet.getRange(текущаяСтрока, результатStartCol).setHorizontalAlignment('center');
    aiSheet.getRange(текущаяСтрока, результатStartCol).setBackground('#D9E1F2');
    заголовокМесяцаRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
    текущаяСтрока++;
    текущаяСтрока++;
    
    // Получаем данные с листа "Все"
    var всеLastRow = всеSheet.getLastRow();
    if (всеLastRow < 1) {
      continue; // Пропускаем месяц, если нет данных
    }
  
    // Колонки на листе "Все":
    // B (2) - Дата платежа
    // C (3) - Номер карты
    // G (7) - Сумма платежа
    // J (10) - Категория
    
    var датаПлатежаCol = 2;  // B
    var номерКартыCol = 3;   // C
    var суммаПлатежаCol = 7;  // G
    var категорияCol = 10;    // J
    
    // Список разрешенных номеров карт
    var разрешенныеКарты = ['*4963', '*5436', '*6058'];
    
    // Названия карт
    var названияКарт = {
      '*4963': '',
      '*5436': 'Красный кошелек',
      '*6058': 'Зеленый кошелек'
    };
    
    // Функция для получения отображаемого названия карты
    function получитьНазваниеКарты(номерКарты) {
      var название = названияКарт[номерКарты] || '';
      if (название) {
        return номерКарты + ' - ' + название;
      }
      return номерКарты;
    }
    
    // Структура данных: данныеПоКартам[номерКарты] = { общаяСумма: число, категории: { категория: сумма } }
    var данныеПоКартам = {};
    
    // Инициализируем данные для каждой разрешенной карты
    for (var i = 0; i < разрешенныеКарты.length; i++) {
      данныеПоКартам[разрешенныеКарты[i]] = {
        общаяСумма: 0,
        категории: {}
      };
    }
    
    // Обрабатываем строки начиная с первой строки
    for (var row = 1; row <= всеLastRow; row++) {
      var датаПлатежа = всеSheet.getRange(row, датаПлатежаCol).getValue();
      var номерКарты = всеSheet.getRange(row, номерКартыCol).getValue();
      var суммаПлатежа = всеSheet.getRange(row, суммаПлатежаCol).getValue();
      var категория = всеSheet.getRange(row, категорияCol).getValue();
      
      // Пропускаем пустые строки
      if (!датаПлатежа || !номерКарты || номерКарты === '' || !категория || категория === '') {
        continue;
      }
      
      // Определяем, какая это карта (ищем разрешенный суффикс)
      var найденнаяКарта = null;
      for (var i = 0; i < разрешенныеКарты.length; i++) {
        if (номерКарты.toString().endsWith(разрешенныеКарты[i])) {
          найденнаяКарта = разрешенныеКарты[i];
          break;
        }
      }
      
      // Пропускаем строки с неразрешенными картами
      if (!найденнаяКарта) {
        continue;
      }
      
      // Преобразуем дату
      var дата = получитьДату(датаПлатежа);
      if (!дата) {
        continue;
      }
      
      // Проверяем, что дата в нужном диапазоне
      if (дата < начальнаяДата || дата > конечнаяДата) {
        continue;
      }
      
      // Получаем сумму
      var сумма = получитьСумму(суммаПлатежа);
      if (сумма === 0) {
        continue;
      }
      
      // Добавляем к общей сумме карты
      данныеПоКартам[найденнаяКарта].общаяСумма += сумма;
      
      // Добавляем к сумме категории для этой карты
      if (!данныеПоКартам[найденнаяКарта].категории[категория]) {
        данныеПоКартам[найденнаяКарта].категории[категория] = 0;
      }
      данныеПоКартам[найденнаяКарта].категории[категория] += сумма;
    }
    
    // Проверяем, есть ли данные для этого месяца
    var естьДанные = false;
    for (var карта in данныеПоКартам) {
      if (данныеПоКартам[карта].общаяСумма !== 0) {
        естьДанные = true;
        break;
      }
    }
    
    if (!естьДанные) {
      continue; // Пропускаем месяц, если нет данных
    }
    
    // Рассчитываем общую сумму всех трат
    var общаяСуммаВсехТрат = 0;
    for (var i = 0; i < разрешенныеКарты.length; i++) {
      var карта = разрешенныеКарты[i];
      общаяСуммаВсехТрат += Math.abs(данныеПоКартам[карта].общаяСумма);
    }
    
    var строкаЗаголовкаТаблицы = текущаяСтрока;
    var строкаНачалаДанных = текущаяСтрока + 1;
    
    // Заголовки таблицы (теперь 3 колонки)
    var заголовкиRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 3);
    aiSheet.getRange(текущаяСтрока, результатStartCol).setValue('Номер карты');
    aiSheet.getRange(текущаяСтрока, результатStartCol + 1).setValue('Общая сумма');
    aiSheet.getRange(текущаяСтрока, результатStartCol + 2).setValue('Процент');
    заголовкиRange.setFontWeight('bold');
    заголовкиRange.setFontSize(12);
    заголовкиRange.setFontFamily('Arial');
    заголовкиRange.setBackground('#4472C4');
    заголовкиRange.setFontColor('#FFFFFF');
    заголовкиRange.setHorizontalAlignment('center');
    заголовкиRange.setVerticalAlignment('middle');
    заголовкиRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
    текущаяСтрока++;
    
    // Данные таблицы общих итогов
    var последняяСтрокаТаблицы = текущаяСтрока;
    for (var i = 0; i < разрешенныеКарты.length; i++) {
      var карта = разрешенныеКарты[i];
      var данные = данныеПоКартам[карта];
      var суммаКарты = Math.abs(данные.общаяСумма);
      var процент = общаяСуммаВсехТрат > 0 ? (суммаКарты / общаяСуммаВсехТрат * 100) : 0;
      
      var строкаRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 3);
      aiSheet.getRange(текущаяСтрока, результатStartCol).setValue(получитьНазваниеКарты(карта));
      aiSheet.getRange(текущаяСтрока, результатStartCol + 1).setValue(суммаКарты);
      aiSheet.getRange(текущаяСтрока, результатStartCol + 1).setNumberFormat('#,##0.00" RUB"');
      aiSheet.getRange(текущаяСтрока, результатStartCol + 2).setValue(процент);
      aiSheet.getRange(текущаяСтрока, результатStartCol + 2).setNumberFormat('0.00"%"');
      строкаRange.setFontFamily('Arial');
      строкаRange.setFontSize(11);
      if (i % 2 === 0) {
        строкаRange.setBackground('#D9E1F2');
      } else {
        строкаRange.setBackground('#FFFFFF');
      }
      строкаRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
      последняяСтрокаТаблицы = текущаяСтрока;
      текущаяСтрока++;
    }
  
    // Пустая строка после таблицы общих итогов
    текущаяСтрока++;
    текущаяСтрока++;
    
    // 2. Выводим категории для каждой карты
    for (var i = 0; i < разрешенныеКарты.length; i++) {
      var карта = разрешенныеКарты[i];
      var данные = данныеПоКартам[карта];
      
      // Получаем категории и сортируем по сумме (по убыванию)
      var категорииКарты = Object.keys(данные.категории);
      if (категорииКарты.length > 0) {
        категорииКарты.sort(function(a, b) {
          return данные.категории[b] - данные.категории[a];
        });
        
        // Заголовок таблицы для карты
        var заголовокКартыRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 2);
        заголовокКартыRange.merge();
        aiSheet.getRange(текущаяСтрока, результатStartCol).setValue('💳 Карта: ' + получитьНазваниеКарты(карта));
        aiSheet.getRange(текущаяСтрока, результатStartCol).setFontWeight('bold');
        aiSheet.getRange(текущаяСтрока, результатStartCol).setFontSize(13);
        aiSheet.getRange(текущаяСтрока, результатStartCol).setFontFamily('Arial');
        aiSheet.getRange(текущаяСтрока, результатStartCol).setHorizontalAlignment('center');
        aiSheet.getRange(текущаяСтрока, результатStartCol).setBackground('#F2F2F2');
        заголовокКартыRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
        текущаяСтрока++;
        
        // Заголовки таблицы категорий
        var заголовкиКатегорийRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 2);
        aiSheet.getRange(текущаяСтрока, результатStartCol).setValue('Категория');
        aiSheet.getRange(текущаяСтрока, результатStartCol + 1).setValue('Сумма');
        заголовкиКатегорийRange.setFontWeight('bold');
        заголовкиКатегорийRange.setFontSize(11);
        заголовкиКатегорийRange.setFontFamily('Arial');
        заголовкиКатегорийRange.setBackground('#FFC000');
        заголовкиКатегорийRange.setFontColor('#000000');
        заголовкиКатегорийRange.setHorizontalAlignment('center');
        заголовкиКатегорийRange.setVerticalAlignment('middle');
        заголовкиКатегорийRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
        текущаяСтрока++;
        
        // Категории с суммами
        for (var j = 0; j < категорииКарты.length; j++) {
          var категория = категорииКарты[j];
          var сумма = данные.категории[категория];
          var строкаКатегорииRange = aiSheet.getRange(текущаяСтрока, результатStartCol, 1, 2);
          aiSheet.getRange(текущаяСтрока, результатStartCol).setValue(категория);
          aiSheet.getRange(текущаяСтрока, результатStartCol + 1).setValue(Math.abs(сумма));
          aiSheet.getRange(текущаяСтрока, результатStartCol + 1).setNumberFormat('#,##0.00" RUB"');
          строкаКатегорииRange.setFontFamily('Arial');
          строкаКатегорииRange.setFontSize(10);
          if (j % 2 === 0) {
            строкаКатегорииRange.setBackground('#FFF2CC');
          } else {
            строкаКатегорииRange.setBackground('#FFE699');
          }
          строкаКатегорииRange.setBorder(true, true, true, true, true, true, '#000000', SpreadsheetApp.BorderStyle.SOLID);
          текущаяСтрока++;
        }
        
        // Пустая строка после таблицы карты
        текущаяСтрока++;
      }
    }
  }
  
  // Автоподбор ширины колонок (3 колонки для таблицы итогов)
  aiSheet.autoResizeColumns(результатStartCol, 3);
  
  SpreadsheetApp.getUi().alert('Суммирование завершено для всех 12 месяцев!');
}

/**
 * Функция для создания меню в Google Sheets
 */
function onOpen() {
  var ui = SpreadsheetApp.getUi();
  ui.createMenu('📊 Финансы')
    .addItem('Суммировать по категориям', 'Test')
    .addToUi();
  }
